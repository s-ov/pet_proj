from django.shortcuts import render

# assignments = EmployeeTaskAssignment.objects.filter(employee=my_employee)
# completed_tasks = assignments.filter(status=EmployeeTaskAssignment.AssignmentStatus.COMPLETED)

